/*
 * Диспетчер прокси-соединений
 *
 * Принимает перенаправленные соединения от tproxy,
 * определяет целевой сервер по политикам маршрутизации,
 * передаёт соединение соответствующему обработчику протокола.
 */

#include <stdio.h>

int dispatcher_start(int max_connections)
{
    /* TODO: запустить epoll-цикл приёма соединений */
    return 0;
}
