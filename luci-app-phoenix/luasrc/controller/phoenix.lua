-- Контроллер LuCI для phoenix-router
-- Регистрирует страницы в меню и связывает их с моделями

module("luci.controller.phoenix", package.seeall)

function index()
    -- TODO: регистрация страниц:
    -- entry({"admin", "services", "phoenix"}, ...)
    -- entry({"admin", "services", "phoenix", "servers"}, ...)
    -- entry({"admin", "services", "phoenix", "routing"}, ...)
    -- entry({"admin", "services", "phoenix", "dns"}, ...)
    -- entry({"admin", "services", "phoenix", "stats"}, ...)
end
